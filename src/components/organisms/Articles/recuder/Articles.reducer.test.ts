import {initialArticlesState} from "./Articles.reducer";

describe('Articles reducer tests', () => {
   test('Toggle Category - turn on second category', () => {
       const state = initialArticlesState;
       const
   })
});